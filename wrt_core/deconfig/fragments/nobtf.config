# AX6600 (jdcloud_re-cs-02) 专用：6MB kernel 分区 (HLOS) 无法容纳 BTF(daed CO-RE) 内核
# 实测数据（2026-08-06）：
#   - 无 BTF 内核 gzip 后 5.50MB  < 6144k ✓（历史成功固件 26.08.06_08.57.57 实测）
#   - 带 BTF 内核 gzip 后 7.51MB  > 6144k ✗（run 31066243038 失败日志实测）
# BTF 数据不可压缩（压缩率 ~0.5%），贡献 ~2MB，6MB 分区装不下；调大 KERNEL_SIZE 会超 bootloader 分区变砖
# 修复：仅本设备关闭 BTF（fragment 追加在 compile_base 之后，覆盖其 =y）
CONFIG_KERNEL_DEBUG_INFO_REDUCED=y
# CONFIG_KERNEL_DEBUG_INFO_BTF is not set
